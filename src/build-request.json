{
  "kind": "build_request",
  "title": "Add business contact information to the Contact section",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-6",
      "text": "Replace the placeholder \"TBD\" contact details in the Contact section with real, configurable business contact information (phone, email, and address), and ensure the phone/email entries render as working clickable links.",
      "target": "frontend",
      "source": {
        "messageIds": [
          "msg-6"
        ],
        "quotes": [
          "Add my contact information in this site"
        ]
      },
      "acceptanceCriteria": [
        "The Contact section no longer displays the literal text \"TBD\" for Phone, Email, or Address.",
        "Phone renders as a clickable link using a valid tel: URL when a phone number is present.",
        "Email renders as a clickable link using a valid mailto: URL when an email address is present.",
        "Address renders as plain text (non-link) unless an explicit address link is provided.",
        "If any contact field is not configured, that line item is hidden (rather than showing placeholders).",
        "All user-facing labels remain in English (e.g., \"Phone\", \"Email\", \"Address\")."
      ]
    }
  ],
  "constraints": [
    "Frontend immutable paths must not be edited: frontend/src/hooks/useInternetIdentity.ts, frontend/src/hooks/useInternetIdentity.tsx, frontend/src/hooks/useActor.ts, frontend/src/main.tsx, frontend/src/components/ui.",
    "Do not add any backend dependency for contact info rendering; contact info must be handled in the frontend."
  ],
  "nonGoals": [
    "Implementing actual inquiry form submission to the backend (the form may remain UI-only).",
    "Adding new authentication or admin UI to edit contact info from the website."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  }
}